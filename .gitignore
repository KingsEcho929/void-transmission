# ✅ Allow encrypted scrolls in outbox
!delivery/outbox/
!delivery/outbox/**/*.age

# 🛡️ Sensitive and ephemeral files (never commit private keys or decrypted payloads)
delivery/keys/
delivery/inbox/
delivery/pings/
delivery/staged/
delivery/immutable/
delivery/logs/
delivery/archive/
delivery/goodfaith/
delivery/veil/
delivery/witness/

# Contacts: track only public keys, veil everything else
!delivery/contacts/
!delivery/contacts/*.pub
# Uncomment if you want to track contacts.json
# !delivery/contacts/contacts.json

# 🧿 Decrypted payloads (local inspection only)
decrypted.txt

# 🧹 System clutter
node_modules/
package-lock.json
*.log
.DS_Store
*.swp
*.swo
*.bak
bak
*.tmp

# 🔐 All keys and unsealed scrolls
delivery/scroll.json
delivery/temp/
delivery/outbox/**/*.json

# 🧱 Final seal: no unencrypted scrolls beyond this line
